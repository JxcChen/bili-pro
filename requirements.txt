# B站视频逐字稿提取系统 - Python依赖列表

# Web框架
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6

# HTTP客户端
aiohttp==3.9.1
httpx==0.25.2
requests==2.31.0

# 数据处理
pydantic==2.5.0
pydantic-settings==2.1.0

# 浏览器自动化
playwright==1.40.0

# 文件处理
aiofiles==23.2.1

# 配置管理
python-dotenv==1.0.0

# 日志
loguru==0.7.2

# 重试机制
tenacity==8.2.3

# 音视频处理
yt-dlp==2023.12.30

# AI 总结服务
openai==1.6.1

# 语音识别 - 云端方案（不需要编译依赖）
# faster-whisper 需要编译 PyAV，在 Render 免费套餐中内存不足
# 生产环境建议使用云端 ASR API（如 bcut-asr 云端版本）

# 必剪ASR (需要单独安装)
# 安装方法:
# git clone https://github.com/SocialSisterYi/bcut-asr
# cd bcut-asr
# poetry lock && poetry build -f wheel
# pip install dist/bcut_asr-*.whl

# 可选 - 任务队列
celery==5.3.4
redis==5.0.1

# 可选 - 数据库
sqlalchemy==2.0.23
alembic==1.13.0

# 开发依赖（可选）
# pytest==7.4.3
# pytest-asyncio==0.21.1
# pytest-cov==4.1.0
# black==23.11.0
# isort==5.12.0
# mypy==1.7.1
# pylint==3.0.2
# flake8==6.1.0